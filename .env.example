# Personal access token from https://api.up.com.au/getting_started
UP_API_TOKEN=up:yeah:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# stdio (default, for local MCP clients) or http (for the container)
# MCP_TRANSPORT=stdio

# Host port compose publishes the HTTP transport on
# MCP_HOST_PORT=8000
